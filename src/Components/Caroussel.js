import data from "../data.json";
const Caroussel = (props) => {
  return (
    <div className="caroussel">
      {data.map((elem, index) => {
        return;
      })}
    </div>
  );
};
export default Caroussel;
